# Build artifacts
*.xpi

# Tooling (the extension itself needs no build)
node_modules/

# macOS / editor
.DS_Store
*.log
*~

# Local AI-assistant notes (developer-only; not for publication)
CLAUDE.md
.claude/
